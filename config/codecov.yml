comment:
  layout: "reach,diff,flags,files"
  behavior: default
  disable: false

coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
