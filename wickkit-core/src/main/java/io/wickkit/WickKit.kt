package io.wickkit

import android.content.Context

object WickKit {

    internal fun init(context: Context) {
        println(context)
        // Subsystems will be initialized here as features are built
    }
}
